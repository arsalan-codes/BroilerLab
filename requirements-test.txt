# Test-only dependencies (NOT installed on Vercel — see api/requirements.txt).
# Usage: pip install -r backend/requirements.txt -r requirements-test.txt
#        python -m pytest tests -q
pytest>=8
httpx>=0.27  # required by fastapi.testclient (starlette)
