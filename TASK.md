# TASK.md — BroilerLab (Ross 308 / Cobb 500 / Arbor Acres+ / Hubbard EP) Feeding-Station Simulator

## Current Objective
شبیه‌ساز آکادمیک چندسویه‌ای دستگاه پایش مصرف خوراک (RFID بال + دو لودسل) با کاتالوگ‌های رسمی Aviagen/Cobb-Vantress/Hubbard، طراح آزمایش کاربرمحور، نقشه فارم انیمیشنی، آزمایشگاه آمار زیستی و رابط دوزبانه FA/EN — متعلق به Arsalan Rezazadeh.

Last Updated: 2026-08-30 — v1.5.20 (ققنوس)

## Active Checklist
- [x] استخراج جدول‌های رسمی Performance Objectives از PDF Aviagen (as-hatched/nr/male/female d0–70)
- [x] دانلود هندبوک 2025 و استخراج دما/نور/RH
- [x] پارامترهای گومپرتز Ross از Marcato 2008 (PDF Redalyc)
- [x] رفتار تغذیه‌ای RFID از van der Sluis 2025 / Li 2018 / Li 2021
- [x] اثر اندازه گروه از Erensoy 2022
- [x] شبیه‌ساز کامل (simulator.py) با schema داده کاربر
- [x] اعتبارسنجی: MAE وزن 0.79% نسبت به PO؛ FI انحراف <4%
- [x] نمودارهای اعتبارسنجی (growth/intake/FCR/diurnal)
- [x] گزارش docs/REPORT.md با Sources راستی‌آزمایی‌شده
- [x] وب‌اپلیکیشن مدرن (webapp/index.html + engine.js + xlsx.js + app.js)
- [x] ۵ نما: داشبورد اعتبارسنجی، شبیه‌سازی زنده دستگاه، سناریوها، دستگاه و داده، مبانی علمی
- [x] صحت‌سنجی موتور JS: MAE=1.09%، FCR پن‌ها 2.24–2.33
- [x] 🧪 طراح آزمایش + 🗺️ نقشه فارم پویا + خروجی Excel + 🧬 کاتالوگ چندسویه‌ای + 📐 آمار زیستی
- [x] i18n کامل فارسی/انگلیسی + RTL-LTR + اعداد بومی‌سازی
- [x] بازطراحی آکادمیک UI v3
- [x] **بک‌اند دستگاه (FastAPI + PostgreSQL + MQTT/WebSocket)** — معماری تاییدشده توسط PM
- [x] **الگوریتم پردازش داده دستگاه**: ثبت ورود RFID + وزن اولیه + timestamp دقیق، محاسبه feed_delta از لودسل مخزن، شمارش بازدید، بستن خودکار وعده
- [x] **مدیریت دوره پرورشی**: ایجاد/حذف دوره (هر دوره داده‌های یونیک) در PostgreSQL با cascade
- [x] **پنل «دستگاه و داده» در فرانت‌اند**: لیست دوره‌ها + ایجاد/حذف + آمار لحظه‌ای + استریم زنده WS
- [x] حذف ایموجی ♻️ از برچسب reset (جایگزین FA icon)
- [x] FontAwesome CDN + لوگوی rooster شفاف در سایزهای استاندارد + favicon
- [x] رفع رندر literal `<br>` در i18n (data-i18n-html)
- [x] تنظیمات دسکتاپ (dropdown) + همبرگر موبایل + تب‌های وسط‌چین + سلکتور سویه مدرن


- [x] ری‌برند BroilerLab → ققنوس (Ghoghnous) — هدر/لندینگ/عنوان/تگلاین یکپارچه `سامانه یکپارچه هوشمند اصلاح نژاد و مدیریت پرورش جوجه‌های گوشتی` (شناسه فنی broilerlab_* حفظ شد)
- [x] هیرو مدرن: mesh سه‌لایه + grid32px + glow emerald/blue + کارت rotate -0.35° + floating `داده زنده · ۲۴ ساعته` پالس + webp 60KB + grid 1.15/.85
- [x] تایپ لوپ عنوان هیرو: 45ms + pause 2600ms + cursor blink + prefers-reduced-motion fallback
- [x] پنل محیط کاربری `#v-workspace` (ws-head/ws-stats/ws-grid 8 کارت) — گیت‌دار + CTA داینامیک `landing-login → مدیریت محیط کاربری` + hide `landing-register`
- [x] تم داینامیک محیط کاربری: `var(--card)` + `html[data-theme=dark|light]`
- [x] انسانی‌سازی i18n: حذف MAE/FCR/RFID/CSV/Pen — جایگزین فارسی رسمی + حذف MQTT
- [x] دیالوگ متریال مدرن `dialog.js` (MDialog.confirm/alert/prompt2) — جایگزین confirm/prompt بومی (بازنشانی/حذف دوره/تغییر رمز)
- [x] بازنشانی کامل با تایید دو-مرحله‌ای: arm 3s + MDialog danger + DELETE /api/cycles cascade + localStorage/charts/inpector/bio صفر
- [x] ریسپانسیو کامل: 320/560/720/900/1020/1040 breakpoints + badge/cta وسط‌چین ≤900/560 + landing-stats 4→2→1 + ws-grid 4→2→1
- [x] پرفورمنس: GZipMiddleware + webapp_server gzip+cache (js/css 1d, png/webp 7d immutable) + defer 9 اسکریپت + idle paint + content-visibility
- [x] دیتابیس: PostgreSQL 5434 — 4 جدول + FK CASCADE (cycles.user_id, visits.cycle_id, device_logs.cycle_id) + isolation per-user verified (admin vs user 404) + fail-closed 401
- [x] صفحه «محصولات و خدمات» عمومی (v-products): ۲ محصول — سیستم پایش مصرف خوراک + سیستم هوشمند کنترل محیطی با مشخصات، ویژگی‌ها، دکمه دمو و تم داینامیک
- [x] حذف حساب پیش‌فرض (admin@broilerlab.local/admin123): دیتابیس پاک شد، seed بک‌اند non-op، متن/placeholder مودال ورود تمیز شد — ثبت‌نام آزاد
- [x] استانداردسازی تایپوگرافی ریسپانسیو کل سایت (v1.5.2): body clamp(13-14.5px)، هیرو clamp fluid (حذف پرش 34→26→22)، تیتر نماها clamp، کف خوانایی 10.5px (۱۲ مورد زیر 10px اصلاح)، هدف لمسی 42-44px در pointer:coarse، جدول‌ها 11px موبایل، ws موبایل
- [x] پاکسازی: حذف .bakها + dead CSS landing-desc-box + ورژن 1.1.0→1.2.0 + DB_PORT default 5434 + webp cache + .gitignore + README
- [x] بازبینی کامل دوزبانه (v1.5.20): افزودن ~60 کلید جدید i18n — لندینگ/هیرو/stats/کارت‌ها/فوت/محیط کاربری/محصولات/auth/device-panel/dialog/toast؛ HTML تمام هاردکدها → data-i18n؛ JS تمام رشته‌ها → window.tr؛ اعتبارسنجی 343 کلید HTML + 57 کلید JS بدون missing

## Blocked — Waiting on Project Manager
- (هیچ)
- **دیپلوی گیت‌هاب**: آماده برای پوش دستی —  `arsalan-codes/BroilerLab` با SSH با خطای `permission denied (publickey)` مواجه است.
  نیاز به یکی از: (A) افزودن کلید عمومی `~/.ssh/id_ed25519.pub` به GitHub SSH keys، یا (B) تغییر remote به HTTPS + token.

## Latest Status
**معماری دستگاه واقعی پیاده‌سازی شد** (درخواست PM): بک‌اند FastAPI + PostgreSQL (پورت ۵۴۳۳ در محیط dev برای جلوگیری از تداخل با کلاستر موجود ۵۴۳۲) + MQTT (paho) + WebSocket (uvicorn[standard]+websockets). الگوریتم پردازش رویدادهای خام دستگاه (۱۲ ستون) پیاده‌سازی و تست شد: ثبت ورود پرنده با RFID → وزن اولیه + timestamp دقیق، محاسبه feed_intake_g از افت لودسل مخزن، شمارش بازدید، بستن خودکار وعده (رویداد exit یا ردیف پایانی یا گپ >۹۰s). دوره‌های پرورشی در جدول `cycles` با cascade کامل به `visits` و `device_logs` مدیریت می‌شوند. فرانت‌اند (`v-dev`) حالا شامل مدیر دوره + استریم زنده WS است. تست انتها‌به‌انتها موفق: REST ایجاد دوره → seed 150 بازدید → stats صحیح (۲۱۰/۲۴۹ بازدید بسته‌شده، age_day نسبت به تاریخ دوره) → WS پوش لحظه‌ای تایید شد.

## Next Action
- رفع بلوک دیپلوی گیت‌هاب (SSH key یا HTTPS) و پوش تغییرات به `main` → GitHub Pages.
- پیشنهادهای بعدی: MQTT broker واقعی (mosquitto) برای دستگاه سخت‌افزاری، Dockerize بک‌اند، README استقرار بک‌اند.

- 2026-08-24: نسخه ۳٫۱ UI/UX — چیدمان کاملاً ریسپانسیو (۳۶۰ تا ۱۴۴۰px بدون سرریز، تست‌شده با Chrome headless در ۶ بیک‌پوینت)، ResizeObserver برای نمودارها، حالت خالی فارم با CTA، دسترس‌پذیری (role/aria/focus-visible/reduced-motion)، و اصطلاح‌شناسی راستی‌آزمایی‌شده فارسی (تنش گرمایی، ضریب تبدیل خوراک، تغذیه همزمان، واحد آزمایشی) + انگلیسی آکادمیک.

- 2026-08-24: v3.2 — سیستم سایه لایه‌ای (shadow-box) روی کارت‌ها/دکمه‌ها/نمودارها/جدول‌ها در هر دو تم + دکمه ♻️ بازنشانی دوره با تأیید دومرحله‌ای (پاک‌سازی زنده/فارم/سناریو/آمار) + دکمه بازگشت طرح آزمایش به پیش‌فرض.

- 2026-08-24: v3.3 — تگ اختصاصی هر پرنده روی نقشه (قابل خاموش/روشن) + کلیک روی هر جوجه → پروفایل فردی (وزن فعلی vs PO جنسیتی، کل مصرف، FCR فردی، CV ژنتیکی، وضعیت) + شیت «Per-bird records» در خروجی اکسل + رفع نقص عملکردی منوی همبرگری (باز می‌ماند، اعضا فعال، ✕/Esc/Backdrop برای بستن) + تکمیل کلیدهای انگلیسی جاافتاده.

- 2026-08-24: v3.4 — تکمیل کامل i18n: ریشه‌یابی ۷۶ نقطه هاردکد فارسی در app.js و مسیردهی به دیکشنری (~۴۰ کلید جدید FA/EN)، بازتولید کامل بخش‌های داینامیک هنگام تعویض زبان (داشبورد/فارم/اینسپکتور/بایو/سناریو/تیکر/طراح)، نام شیتهای اکسل به انگلیسی استاندارد + برچسب تیمارها دوزبانه در موتور (labelEn). امنیت: پاک‌سازی charset شناسه پن (موتور+UI)، escape همه interpolationهای HTML، گارد تزریق فرمول اکسل/CSV، ممیزی eval/XSS/noopener. تست با کلیک ماوس واقعی puppeteer: EN scan=۰ باقی‌مانده فارسی، XSS/CSV ✓، کل رگرسیون‌ها سبز.

- 2026-08-24: v1.0.0 نهایی — تب روش‌شناسی آکادمیک (چکیده، خط تولید، معادلات، ماتریکس صحت ۴ سویه، آمار، عدم‌قطعیت، تکرارپذیری) + انتخاب ستون‌های خروجی + رفع باگ click-blocker آموزش + i18n EN کامل.

- 2026-08-25: v1.0.0 نهایی — تب روش‌شناسی آکادمیک (چکیده، خط تولید، معادلات، ماتریکس صحت ۴ سویه، آمار، عدم‌قطعیت، تکرارپذیری) + انتخاب ستون‌های خروجی + رفع باگ ریشه‌ای click-blocker آموزش + i18n EN کامل + فلش RTL + HTML 깔끔.

## Summary History
- 2026-09-03: v1.8.25 — URLهای تمیز بدون `#` (روتر History-API: pushState/popstate، canonical مطلق `/feed`، alias اسلشی `/dash` + شناسه `v-dash`، مهاجرت لینک‌های قدیمی `#/…`، base خودکار `/BroilerLab` برای Pages، rewrite صریح Vercel + فالبک `404.html` برای Pages) + فیکس منوی موبایل (تاپ آواتار در ≤992px دراور را باز می‌کند) + فیکس فالبک SPA سرور لوکال. تست: ۲۳ passed و ۳ skipped + ماتریس زنده مرورگر (دیپ‌لینک/ناوبری/back/لینک قدیمی/برند/دراپ‌داون دسکتاپ/دراور موبایل). کامیت‌ها `0ba6d62` + `59d98f1` روی main پوش شد. وضعیت دیپلوی: Pages (auto) در حال انتشار؛ Vercel prod هنوز 1.8.23 — نیاز به redeploy دستی (توکن در سشن نیست).
- 2026-09-04: v1.8.25-live — علت `#` زنده در پروداکشن پیدا و فیکس شد: فایل‌های ریشه ورسل (`router.js/auth.js/version.js`) قدیمی 1.8.23 بودند در حالی‌که آینه `webapp/` تمیز بود (تست فقط آینه را پین می‌کرد). هر سه فایل از سورس همگام شد + تست جدید `test_deploy_root_parity` (ریشه==سورس). کامیت `929979f` پوش شد؛ دیپلوی پروداکشن READY و alias خورد. وریفای زنده: `version.js=1.8.25`، `/feed=200`، روتر بدون `#/…`، `auth.js` دارای `openMobileDrawer` (دراور موبایل زنده).
- 2026-09-04: v1.8.27 — فیکس «آواتار موبایل منوی همبرگری باز می‌کند»: سه علت پشت سر هم — (۱) پنل کاربری داخل دراور بسته می‌ماند → بازشوی خودکار+اسکرول+`aria-expanded`؛ (۲) حباب کلیک به closer سراسری بلافاصله می‌بست → `stopPropagation`؛ (۳، اصلی) قانون سراسری `.auth-area-drawer{display:none}` روی `display:block` موبایل سایه می‌انداخت و پنل هیچ‌وقت دیده نمی‌شد → انتقال به `@media(min-width:993px)`. تست‌ها: ۲۷ passed و ۳ skipped (+۳ تست موبایل). پروب زنده DOM: دراور باز + پنل ۱۹۳px با هر ۳ گزینه. کامیت `ad8923f` پوش شد؛ دیپلوی پروداکشن ورسل (`dpl_HKhsaMi4`) READY و alias خورد — وریفای زنده: `version.js=1.8.27`، `/feed=200`، بدون `href="#/…"`، `auth.js` با فیکس.
- 2026-09-04: v1.8.28 — تفکیک موبایل «کاربر تمام‌صفحه vs همبرگر»: تاچ آواتار در ≤992px دراور را در حالت `drawer-user` باز می‌کند — شیت تمام‌عرض (۱۰۰٪) فقط با پنل کاربری بازشده (مدیریت محیط/تغییر رمز/خروج) و مخفی‌بودن زبان/پوسته/راهنما/آمار؛ دکمه برگر در حالت `drawer-nav` فقط زبان/پوسته/راهنما/بازنشانی را نشان می‌دهد و پنل کاربری مخفی است (`:has(.auth-user)`). ریشه مشکل: پیاده‌سازی در درخت dev آماده بود ولی آینه‌های دیپلوی عقب بودند + دو تست روی کوتیشن `user` تناقض داشتند (تست‌ها quote-agnostic شدند). تست‌ها: ۲۸ passed و ۳ skipped. پروب زنده DOM (عرض ۸۰۰): آواتار → شیت ۷۸۵px + پنل ۱۹۳px با ۳ گزینه؛ برگر → منوی تنظیمات بدون پنل کاربری. کامیت `590254e` روی main پوش شد (Pages خودکار)؛ ورسل نیاز به redeploy دستی دارد.
- 2026-09-04: v1.8.28-live — دیپلوی پروداکشن ورسل با توکن قبلی کاربر انجام شد (READY + alias روی `ariansense.vercel.app`). وریفای زنده: `version.js=1.8.28`، `/feed=200`، بدون `href="#/…"`، `auth.js` دارای `openDrawer('user')`، `app.js` دارای `openDrawer(mode)`، CSS شامل `drawer-user/drawer-nav`. توکن فقط در سشن استفاده و با `shred`+`unset` پاک شد.
- 2026-09-04: v1.8.29 — پنل کاربری موبایل شد کشوی جدا از سمت چپ (عرض 340px، گوشه گرد، سایه، انیمیشن اسلاید): هدر پروفایل گرادیانی (آواتار 58px + نام)، ایمیل وسط‌چین، سه ردیف لمسی 52px (مدیریت محیط/تغییر رمز/خروج). باگ لفت‌چسبی: inset-inline-end:auto منطقی روی left:0 فیزیکی سایه می‌انداخت و پنل وسط صفحه می‌افتاد → فقط left:0/right:auto فیزیکی. تست‌ها: 28 passed و 3 skipped. پروب زنده DOM (عرض 800، RTL): آواتار → پنل چپ 340px + انیمیشن translateX از 112٪-؛ برگر → منوی ناوبری بدون تغییر. کامیت 43ebb6c روی main پوش شد (Pages خودکار)؛ ورسل نیاز به redeploy دستی دارد.
- 2026-09-04: v1.8.29-live — دیپلوی پروداکشن ورسل انجام شد (READY + alias روی ariansense.vercel.app). وریفای زنده: version.js=1.8.29، /feed=200، بدون href="#/..."، استایل هدر پروفایل و اسلاید چپ زنده. توکن فقط در سشن استفاده و با shred+unset پاک شد.
- 2026-09-04: v1.8.30 — کلیک موبایل دقیقاً مثل دسکتاپ شد: دکمه قرصی (آواتار+نام+شورون) همان کارت دراپ‌داون شناور را تاگل می‌کند (head + سه گزینه)؛ حالت شیت چپ (drawer-user + openMobileDrawer) حذف شد؛ دراور فقط با برگر برای منوی ناوبری باز می‌شود. تست‌ها: 28 passed و 3 skipped. پروب زنده (عرض 800): آواتار → دراپ‌داون 200px زیر دکمه بدون کلاس دراور؛ برگر → دراور ناو 330px. کامیت 8c30009 روی main پوش شد (Pages خودکار)؛ ورسل نیاز به redeploy دستی دارد.
- 2026-09-04: v1.8.31 — برگشت به حالت قبلی: شیت چپ کاربر (340px از چپ، هدر پروفایل، سه گزینه) برگشت؛ تغییر دسکتاپی v1.8.30 revert شد. نسخه 1.8.31 (جلو، برای bust کش). تست‌ها: 28 passed و 3 skipped. پروب زنده: آواتار → دراور drawer-user چپ 340px + پنل باز. کامیت f28ec9f روی main پوش شد (Pages خودکار)؛ پروداکشن ورسل هنوز 1.8.29 (همان طراحی) است و نیازی به redeploy ندارد.
- 2026-09-04: v1.8.32 — باتم‌شیت حساب موبایل: تاچ آواتار شیت پایین‌رونده (دسته درگ، هدر پروفایل 84px، منوی 5 ردیفه، خروج، ردیف نسخه+سوییچ تم تیره، اورلی تیره، بستن با سوایپ/تاپ-بیرون/Escape، RTL/LTR، safe-area). همه ردیف‌ها به اکشن موجود وصل‌اند (workspace/change-pass/nav-drawer/help/v-about)؛ دسکتاپ و برگر دست‌نخورده. کلیدهای sheet.* در fa/en. تست‌ها: 15/15 clean-urls سبز (device_table E2E از قبل به‌خاطر env خراب است، بدون رگرسیون). پروب زنده: شیت چسبیده به پایین، سوییچ تم dark→light، بستن با بک‌دراپ، برگر drawer-nav. کامیت 06e310e روی main (Pages خودکار)؛ ورسل نیاز به redeploy با توکن دارد.
- 2026-09-04: v1.8.32-live — دیپلوی پروداکشن ورسل با توکن کاربر انجام شد (dpl_9Vqi... READY + alias روی ariansense.vercel.app). وریفای زنده: version.js=1.8.32، /feed=200، بدون href="#/..."، کانتینرهای account-sheet/sheet-backdrop زنده. توکن فقط در سشن استفاده و با shred+unset پاک شد.
- 2026-09-04: v1.8.33 — راهنمای تعاملی تور: فیکس شید (tap-outside + بلاک پس‌زمینه)، توکن ضد race قدم‌ها، چسبیدن تیپ به هدف در اسکرول/ریسایز، انیمیشن ورود جهت‌دار + پالس رینگ، داک پایین شیت‌مانند در ≤560px با safe-area، reduced-motion، کیبورد. تست‌ها: 7 تست تور + 15 clean-urls سبز (device_table E2E از قبل env-خراب). پروب زنده: تیپ tt-below، چسبیدگی، بستن با شید و Escape. کامیت 70a98b8 روی main (Pages خودکار)؛ ورسل نیاز به redeploy با توکن دارد.
- 2026-09-04: v1.8.33-live — دیپلوی پروداکشن ورسل با توکن کاربر انجام شد (READY + alias روی ariansense.vercel.app). وریفای زنده: version.js=1.8.33، /feed=200، بدون href="#/..."، app.js?v=1.8.33 و tour-tip زنده. توکن فقط در سشن استفاده و با shred+unset پاک شد.
- 2026-09-04: v1.8.34 — دراور همبرگر مدرن چپ‌چسب (هر دو جهت)، پخش space-between (+safe) با پیل‌های سنجاق به پایین، لیبل فارسی/انگلیسی ردیف راهنما (hdr.help، دسکتاپ آیکون-only). باگ cascade: سنترینگ سراسری topbar روی موبایل غلبه می‌کرد → اورراید هم‌اختصاصی + تست پین. تست‌ها: 4 دراور + 22 قبلی سبز (device_table E2E از قبل env-خراب). پروب زنده: left:0، justify:space-between، لیبل آموزش سریع. کامیت 52d0375 روی main (Pages خودکار)؛ ورسل نیاز به redeploy با توکن دارد.
- 2026-09-04: v1.8.34-live — دیپلوی پروداکشن ورسل با توکن کاربر انجام شد (READY + alias روی ariansense.vercel.app). وریفای زنده: version.js=1.8.34، /feed=200، بدون href="#/..."، لیبل راهنما زنده. توکن فقط در سشن استفاده و با shred+unset پاک شد.
- 2026-09-04: v1.8.35 — حذف کامل همبرگر: برگر/دراور/بک‌دراپ/dhead/آینه دراور از مارکاپ و CSS و JS رفت (704- خط)؛ دسکتاپ با کنترل‌های inline سالم ماند، موبایل hctl مخفی. محتویات در دراپ‌داون کاربر (زبان/پوسته/راهنما/بازنشانی با setLang/setTheme موجود) + شیت (کارت زبان inline + ردیف بازنشانی). باگ bindReset (نابودی لیبل + ایموجی) فیکس شد؛ اسکرول تاچ (momentum/touch-action/overscroll) تنظیم شد. تست‌ها: 41 سبز (device_table E2E از قبل env-خراب). پروب زنده: بدون برگر، تغییر زبان به EN+LTR، arm بازنشانی با حفظ لیبل، تم از دراپ‌داون. کامیت e590b7a روی main (Pages خودکار)؛ ورسل نیاز به redeploy با توکن دارد.
- 2026-09-04: v1.8.35-live — دیپلوی پروداکشن ورسل با توکن کاربر انجام شد (READY + alias روی ariansense.vercel.app). وریفای زنده: version.js=1.8.35، /feed=200، بدون href="#/..."، بدون nav-burger. توکن فقط در سشن استفاده و با shred+unset پاک شد.
- 2026-09-04: v1.8.36 — فیکس اسکرول انگشتی شیت: علت flex-shrink پیش‌فرض بچه‌های ستون فلکس بود (محتوا فشرده می‌شد، سرریز رخ نمی‌داد)؛ پین قد طبیعی + مومنتوم iOS. تست قرارداد + پروب زنده (محتوای 752px در شیت 220px اسکرول شد). 42 سبز (device_table E2E از قبل env-خراب). کامیت a895fc4 روی main (Pages خودکار)؛ ورسل نیاز به redeploy با توکن دارد.
- 2026-09-04: v1.8.36-live — دیپلوی پروداکشن ورسل انجام شد (READY + alias روی ariansense.vercel.app، توکن از فایل ~/.config/arian/vercel_token خوانده شد). وریفای زنده: version.js=1.8.36، /feed=200، بدون href="#/..."، بدون nav-burger.
- 2026-09-04: v1.8.37 — محیط کاربری: دکمه تغییر رمز + کلیک روی تعداد دوره‌ها → پنل مدیریت دوره‌ها (ریست کامل داده هر دوره + حذف مجزا با تایید خطرناک). اندپوینت fail-closed جدید DELETE /cycles/{id}/data (E2E واقعی: ایزوله 401/404، ریست 1/1، حفظ دوره). شیت: ردیف‌های تنظیمات/بازنشانی حذف شد. تور: سوراخ اسپات‌لایت جایگزین z-index شد (بلور اهداف رفع شد). فیکس ریشه‌ای bcrypt (رجیستر/لاگین 500 بود). 49 سبز 0 قرمز. کامیت e6348b6 روی main (Pages خودکار)؛ ورسل با توکن ذخیره‌شده redeploy می‌خواهد.
- 2026-09-04: v1.8.37-live — دیپلوی پروداکشن ورسل با توکن ذخیره‌شده انجام شد (READY + alias). پاکسازی: 93 دیپلوی قدیمی حذف شد، فقط نسخه زنده ماند. وریفای زنده: version.js=1.8.37، /feed=200، root=200.
- 2026-09-04: v1.8.38 — کارت‌های لندینگ کلیک‌شدنی شدند (data-go به ویوی اختصاصی + کیبورد + هوور). پروب زنده: بی‌ورود → مودال لاگین، باورود → v-dev با URL تمیز /device. 51 سبز 0 قرمز. کامیت 42a12c1 روی main (Pages خودکار)؛ ورسل با توکن ذخیره‌شده redeploy می‌خواهد.
- 2026-09-04: v1.8.39 — فاصله برند: margin دائمی بین «سامانه هوشمند» و «آرین» (۴→۸px، هر دو جهت؛ تست پین).
- 2026-09-04: v1.8.40-live — تاپ‌بار موبایل full-bleed (منفی‌مارژین 14px، L=R=0 واقعی) + ساعت روی‌هم 10.5px؛ ورسل READY (dpl_EY415uW8WNqVsuNbyxkNHgixgUc7، teamId قدیمی 403 بود — team از توکن کشف شد)، prod_ver=1.8.40، root/feed 200، سنتینل CSS زنده.
- 2026-09-04: v1.8.41-live — ساعت موبایل بدون حاشیه/پس‌زمینه/سایه + حذف نقطه سبز پالس (دسکتاپ دست‌نخورده). ورسل READY (dpl_CBsFrkzMUYioYuGyUF5bfNBCT3eZ)، prod_ver=1.8.41، سنتینل‌های CSS زنده ۱/۱، root/feed 200.
- 2026-09-04: v1.8.42 — خط جداکننده بین تاریخ و ساعت (28×1px). ۵۵ سبز، کامیت 4afcc2e روی main (Pages خودکار). ورسل BLOKED: 402 Payment Required روی همه تلاش‌ها (حساب Hobby فعال ولی API دیپلوی نمی‌پذیرد) — منتظر رفع سمت ورسل یا توکن/حساب سالم؛ کد روی main آماده است.
- 2026-09-04: v1.8.44 — فاصله هیرو لندینگ: دو ریشه (gap ستونی فقط row بود + قاعده display:block با :not شکست‌دهنده inline-flex)؛ حالا gap:.04em .22em و بلاک‌قاعده .brand-hero را مستثنا کرد. اندازه‌گیری زنده: 13px (0.226em) در FA/EN. ۵۶ سبز، کامیت e2f1d9f (ورسل همچنان منتظر ریست سهمیه).
- 2026-09-04: v1.8.47 — تایپوگرافی هیرو یک‌فرمولی: clamp(34px,6.1vw,72px) از اندازه‌گیری واقعی گلیف (EN 13.9em)؛ تیر ≤560 حذف شد؛ FA تا 560px و EN تا 860px تک‌خط، زیر آن wrap متوازن. ۶۰ سبز، کامیت b9367e4 (ورسل منتظر ریست سهمیه).
- 2026-09-04: v1.8.48 — تب «روش‌شناسی» (v-met) به mainnav اضافه شد (بین sci و products)؛ gated مثل قبل. پروب زنده: بی‌ورود dim 0.55، باورود /methodology سبز. ۶۳ تست، کامیت 0e728c6.
- 2026-09-04: v1.8.49 — سیستم کنترل محیطی: تب v-env (۹ تایل زنده: دما/رطوبت/بستر/خوراک/آب/آمونیاک/اکسیژن/فن/نور)، نمودار چند-سالنه، سلامت گله (بینایی ماشین)، جدول سخت‌افزار، اکسل (ساعتی/روزانه/ماهانه/بازه). بک‌اند: جدول EnvSample (پیام MQTT=یک insert، ایندکس house_id+ts)، fast-path MQTT برای broilerlab/env/<house>، /api/env/summary + /api/env/export fail-closed. تب روش‌شناسی از group feed جدا شد. ۶۳ سبز، dd03863.
- 2026-09-04: v1.8.50 — بازطراحی گزارش تغییرات: scope-picker قرصی (ساعتی/روزانه/ماهانه/بازه) به سبک langseg، فیلدهای بازه فقط برای «بازه دلخواه»، دکمه اصلی گرادیانی سایت + shine، خط وضعیت aria-live با اسپینر/موفق/خطا. پروب زنده سبز (تعویض scope و busy). ۶۳ تست، کامیت bfb0d5e.
- 2026-09-05: v1.8.52-live — تنظیمات تاریخ/منطقه زمانی (فرمت تقویم auto|شمسی|میلادی + ۸ منطقه زمانی؛ auto تابع زبان) + فیکس ریشه‌ای: Intl روی fa-IR به‌طور پیش‌فرض تقویم شمسی است — calendar صریح گره خورد. ۶۷ سبز؛ ورسل READY (dpl_GYVEif7gSe3ue5JcAnB1Txffi3tU)، prod_ver=1.8.52، سنتینل‌ها ۱/۱، root/feed/env 200.
- 2026-09-05: v1.8.54-live — dd-select.js: همه selectهای سایت (تنظیمات، آزمایش، فارم، بیواستت، شبیه‌سازی، سناریو، خروجی + ردیف‌های پویا با MutationObserver) به ظاهر strain-select تبدیل شدند؛ native select مخفی منبع truth است. ۶۷ سبز؛ ورسل READY (dpl_Hmj6FunVSMjwUwLFpg1iYu8YaChr)، prod_ver=1.8.54.
- 2026-09-05: v1.8.55 — پدینگ hero-title (.08em .12em .1em .04em)؛ بدون overflow. ۶۷ سبز، 975d18e.
- 2026-09-05: v1.8.56 — پدینگ کامل حروف در brand-hero-accent (.12em .18em .2em .16em؛ جای دیاکریتیک بالا و دُم پایین) — گرادیان متن را کامل در بر می‌گیرد. ۶۷ سبز، bb82c15.
- 2026-08-23: نسخه اول کامل — تحقیق ۸ منبع معتبر + شبیه‌ساز + اعتبارسنجی 0.79% MAE + گزارش.
- 2026-08-23: وب‌اپلیکیشن RossSim — موتور JS وفادار به simulator.py + طراحی مدرن RTL + سناریوها.
- 2026-08-23: نسخه ۲ — طراح آزمایش (پن/تعداد/تیمار)، نقشه فارم پویا، خروجی Excel+CSV.
- 2026-08-23: نسخه ۳ BroilerLab — کاتالوگ ۴ سویه از منابع رسمی، آمار زیستی کامل، دوزبانه FA/EN، مالکیت Arsalan Rezazadeh، بازطراحی آکادمیک.
- 2026-09-17: online ingest دستگاه توزین uktech — دکمه «دریافت داده دستگاه» در v-dev: pull از api_weight_data.php (سریال ESP800، کرسر افزایشی sync_state، idempotency با external_id یکتا، ساعت تهران→UTC، گردکردن اعشار) به چرخه انتخابی کاربر؛ اندپوینت‌های POST /api/uktech/sync + GET /api/uktech/status (توکن فقط env سمت سرور) + auto-poll اختیاری لوکال + migration 004 + تست test_uktech (۷ سبز). تست زنده: 200 رکورد → 19 بازدید/۲ پرنده، sync مجدد ۰ رکورد. برای پروداکشن: UKTECH_API_TOKEN در env ورسل + UKTECH_VERIFY_SSL=false اگر زنجیره TLS رد شد.
- 2026-09-18: weighing-session filter — ماشین حالت per-(device,rfid): EMPTY→DETECTING→REGISTERED→STABLE→WAITING_FOR_EMPTY؛ فقط رویداد تأییدشده ویزیت می‌سازد (residual مثل 6.77 و صفرها فقط raw log)، ratchet صعودی در باند، anti-duplicate، timeout ضدگیرکردن، کانفیگ UKTECH_*، normalize نمایشی round2، بج وضعیت + اندپوینت sessions، migration 007. تست: ۱۵ واحد + ۲ integration (سیکوئنس‌های acceptance)؛ جدول فقط رویداد معتبر نشان می‌دهد.
- 2026-09-19: live per-unit core — جایگزینی فیلتر به‌عنوان گیت: هر رکورد دو یونیت مستقل (CHANNEL_MAP تأییدشده: یونیت ۱ rfid1/weight_2(status1)/weight_1/calib2/1، یونیت ۲ rfid2/weight_4/weight_3/calib4/3) از کانال‌های خام (بدون round) با idempotency نسل‌دار `g<gen>`؛ هسته واحد `process_unit_sample` (یک تابع، دو منبع: API poll + پوش مستقیم ESP/MQTT) با آستانه‌های ۱۵g/D2؛ وزن زنده + خوراک هر سیکل + elapsed ترکیبی (counter VALID→VALID و fallback دیواری) و presence؛ مکث VALID، خروج نهایی در همان سطر و badge موقعیت inside/outside؛ یونیت ۱/۲ و badge در جدول + selector منبع (api/direct با 409). Reset دیتابیس: بستن interrupted بدون حذف تاریخ. Migration 013 (Visit live cols + Cycle ingest_source + UnitState). Poll ۳s با تک‌فلایت و سقف صفحه/tick. بازسازی dry-run: `python -m backend.rebuild --cycle <id> [--apply]`. Fix: کاهش غیردرمانی گردهای bin/calib، مدت elapsed و نمایش وزن زنده. تست: ۲۴ core + بروزرسانی ۴ integration (WHY مستند).
- 2026-09-19: قوانین جدید مالک (status1=یونیت ۱، status2=یونیت ۲) — وزن بر پرچم مقدم است: w2/w4 صفر (یا پسماند ≤۱۵g) = مرغ خارج، مستقل از پرچم (دستگاه خودش سطرهای صفر را هم VALID و هم INVALID می‌فرستد) → استریک خروج هر-پرچمی؛ INVALID = مرغ داخل ولی نمی‌خورد (موتور تا ۳۰s خارج می‌کند) → ورود INVALID ویزیت را PAUSED باز می‌کند (elapsed 0 تا جفت VALID، resume روی VALID، بستن روی صفر) و بج ⏸ با tooltip تخلیه؛ شیب تخلیه (پرش تک‌مرحله‌ای > UKTECH_BIRD_JUMP_G=30g) وزن زنده را فریز می‌کند تا final وزن واقعی بماند (ویریت ۳۴۰ قبلاً ۴۲.۲۸ نشان می‌داد) — فیکس stale-clear در مسیر update + تست‌های جدید (weight-jump/invalid-zero/invalid-entry). هر poll می‌نویسد: رکورد جدید → DeviceLog+Visit یک ترنزکشن، کرسر sync_state هر poll بروز (حتی خالی)، جداول در بوت self-heal. v1.8.81.
- 2026-09-19: قانون خروج ۳۰ ثانیه‌ای INVALID — INVALID پیاپی ≥ UKTECH_INVALID_EJECT_S=30s یعنی موتور مرغ را خارج کرده: ویزیت در همان سطر finalizes می‌شود (position=outside، reason=ejected، exit_ts=invalid_since+30، elapsed/feed/presence فریز در آخرین VALID — بازه INVALID هرگز جمع نمی‌شود) و داده از API همچنان می‌آید. استریک فقط روی رکوردهای INVALID پیاپی اندازه‌گیری می‌شود (گلیچ تکی + gap نامنظم هرگز پرنده خوار را eject نمی‌کند؛ resume روی VALID). شمارش از همان ورود INVALID شروع می‌شود (paused-open). ستون `Visit.invalid_since` + migration 014 (پایدار در سرورلس restart) + self-heal. بازسازی dry-run حالا state را بین رکوردها حمل می‌کند (streak/eject در replay درست کار می‌کند — باگ ساختاری فیکس شد). تست: ۳۰ core (eject/flap/zero/paused-entry). v1.8.82.
- 2026-09-20: ماشین حالت صریح کسب‌وکار — `business_state` جدا از پرچم خام (EMPTY/FEEDING/EJECTING/EXITED/UNIDENTIFIED/STALE) روی Visit و UnitState؛ `invalid_deadline` persisted (restart شمارش را ریست نمی‌کند) + sweep تنبل در شروع هر سینک/ingest (finalize سررسیده حتی با صفر رکورد جدید)；فیلدهای جدا (live/last_valid/initial_bin/final_bin/weight_gain/last_source) — هیچ ستونی دو معنا ندارد؛ ایندکس `uq_visit_open_lane` ضد ویزیت باز تکراری (concurrency در سطح DB + dedupe legacy)؛ لاگ‌های ساخت‌یافته `[UNIT n] RFID=…`؛ بج وضعیت با شمارش معکوس سرور (`eject_in_s`)؛ مشکل thead موبایل (sticky + اسکرول یکپارچه)؛ داک `docs/LIVE_CORE.md` + env های جدید در DEPLOY.md. Migration 015. v1.8.84.
- 2026-09-21: v1.8.85 — پنل سخت‌افزاری — دکمه «پنل سخت‌افزاری» در صفحه device (کنار «دریافت داده دستگاه») که داشبورد سخت‌افزاری uktech را در تب جدید باز می‌کند (`device_weight_dashboard.php?serial=ESP800`)؛ سریل از `/api/uktech/status` همگام می‌شود (fallback همان ESP800)، i18n دو زبانه + tooltip، لینک anchor با rel=noopener. Mirror کامل (webapp/ + 404.html).
- 2026-09-21: v1.8.86 — شکار جامع باگ (بک‌اند + فرانت + مهاجرت‌ها). بک‌اند: ترتیب persist سینک (UPDATE بسته‌ها قبل از INSERT بازدید جدید — نقض uq_visit_open_lane در چانک close+open ۵۰۰ می‌داد و کرسر را مسموم می‌کرد)؛ گارد IntegrityError در سه نقطه سینک → نتیجه stalled-shaped با کرسر دست‌نخورده (retry بعدی همگرا می‌شود، بدون ۵۰۰)؛ `_ingest_one` فقط تکرار واقعی (uq_log_cycle_external) را duplicate گزارش می‌کند — سایر conflicts → 503 retryable (قبلاً هر IntegrityError رویداد را بی‌صدا گم می‌کرد)؛ گارد resurrect در persist (live/close mapping برای سطرهای بسته‌شده توسط ورکر موازی رد می‌شود)؛ حذف بلوک swap-reopened تکراری (events_count دوبار جمع می‌شد)؛ **ریشه‌ای: `visits.device_id` هیچ‌وقت وجود نداشت** — ایندکس `uq_visit_open_lane` و dedupe به sensor_id اصلاح شد (مدل + migration 015 + self-heal) و ایندکس روی `Visit.__table_args__` هم declare شد (create_all تازه زیر همان گارد)؛ مسیر alembic در init_db حالا create_all additive می‌کند (env_samples روی دیتابیس alembic تازه ۵۰۰ می‌داد)؛ `reset_cycle_data` حالا unit_states + کرسرهای sync را هم پاک می‌کند (قبلاً phantom lane + STALLED ابدی)؛ DeviceStatusIn.active nullable (بدنه {} دستگاه را ناخواسته فعال نمی‌کند)؛ env/summary با `_aware_dt` (naive/aware روی SQLite ۵۰۰ می‌داد). فرانت: `ukBackoffMs` ( ReferenceError هر poll بعد از خطا)؛ گیت اولیه `arian_token || broiler_token`؛ `rossim:view` → `arian:route` (هرگز dispatch نمی‌شد — پولینگ موقع خروج از device نمی‌ایستاد)؛ `window.clearDevicePanel` تعریف شد (تایمر/WS در ریست workspace)؛ 401 → clearAuth (مودال بی‌نهایت)؛ گارد پاسخ stale روی loadStats/loadRegistrations/سینک (تعویض سریع دوره داده قدیمی می‌ریخت)؛ startClock گارد اینترال (۳ تیک پشته‌ای)؛ داده خراب strains.js ترمیم شد (aaplus d31–36 FI هر سه جنسیت + FCR nulls؛ cobb500 d1–7 FI — interpolation بین همسایه‌های سالم، provNote مستند)؛ دکمه‌های export خروجی CSV/اکسل i18n شدند؛ dialog aria-hidden + ریس رفع شد؛ env-control: 401 → حالت offline (نه داده دمو) + encode from/to + کلید `env.metric.vision`؛ وب‌سرور لوکال commonpath cross-drive → 404. تحلیل‌شده و ردشده (باگ نبودند): last_source_* روی stale-sample (طبق داک طراحی)، تاریخ تهران shamsi (لنگر/کبیسه/نرمال‌سازی ورسفای شد)، شنونده‌های click در auth.js (ensureModal گارد دارد)، dateForAge موتور. تست: 152 passed و 6 skipped.
