# TASK.md — BroilerLab (Ross 308 / Cobb 500 / Arbor Acres+ / Hubbard EP) Feeding-Station Simulator

## Current Objective
شبیه‌ساز آکادمیک چندسویه‌ای دستگاه پایش مصرف خوراک (RFID بال + دو لودسل) با کاتالوگ‌های رسمی Aviagen/Cobb-Vantress/Hubbard، طراح آزمایش کاربرمحور، نقشه فارم انیمیشنی، آزمایشگاه آمار زیستی و رابط دوزبانه FA/EN — متعلق به Arsalan Rezazadeh.

Last Updated: 2026-08-23

## Active Checklist
- [x] استخراج جدول‌های رسمی Performance Objectives از PDF Aviagen (as-hatched/nr/male/female d0–70)
- [x] دانلود هندبوک 2025 و استخراج دما/نور/RH
- [x] پارامترهای گومپرتز Ross از Marcato 2008 (PDF Redalyc)
- [x] رفتار تغذیه‌ای RFID از van der Sluis 2025 / Li 2018 / Li 2021
- [x] اثر اندازه گروه از Erensoy 2022
- [x] شبیه‌ساز کامل (simulator.py) با schema داده کاربر
- [x] اعتبارسنجی: MAE وزن 0.79% نسبت به PO؛ FI انحراف <4%
- [x] نمودارهای اعتبارسنجی (growth/intake/FCR/diurnal)
- [x] گزارش docs/REPORT.md با Sources راستی‌آزمایی‌شده
- [x] وب‌اپلیکیشن مدرن (webapp/index.html + engine.js + xlsx.js + app.js)
- [x] ۵ نما: داشبورد اعتبارسنجی، شبیه‌سازی زنده دستگاه، سناریوها، دستگاه و داده، مبانی علمی
- [x] صحت‌سنجی موتور JS: MAE=1.09%، FCR پن‌ها 2.24–2.33
- [x] 🧪 طراح آزمایش + 🗺️ نقشه فارم پویا + خروجی Excel + 🧬 کاتالوگ چندسویه‌ای + 📐 آمار زیستی
- [x] i18n کامل فارسی/انگلیسی + RTL-LTR + اعداد بومی‌سازی
- [x] بازطراحی آکادمیک UI v3
- [x] **بک‌اند دستگاه (FastAPI + PostgreSQL + MQTT/WebSocket)** — معماری تاییدشده توسط PM
- [x] **الگوریتم پردازش داده دستگاه**: ثبت ورود RFID + وزن اولیه + timestamp دقیق، محاسبه feed_delta از لودسل مخزن، شمارش بازدید، بستن خودکار وعده
- [x] **مدیریت دوره پرورشی**: ایجاد/حذف دوره (هر دوره داده‌های یونیک) در PostgreSQL با cascade
- [x] **پنل «دستگاه و داده» در فرانت‌اند**: لیست دوره‌ها + ایجاد/حذف + آمار لحظه‌ای + استریم زنده WS
- [x] حذف ایموجی ♻️ از برچسب reset (جایگزین FA icon)
- [x] FontAwesome CDN + لوگوی rooster شفاف در سایزهای استاندارد + favicon
- [x] رفع رندر literal `<br>` در i18n (data-i18n-html)
- [x] تنظیمات دسکتاپ (dropdown) + همبرگر موبایل + تب‌های وسط‌چین + سلکتور سویه مدرن

## Blocked — Waiting on Project Manager
- (هیچ)
- **دیپلوی گیت‌هاب**: پوش به `arsalan-codes/BroilerLab` با SSH با خطای `permission denied (publickey)` مواجه است.
  نیاز به یکی از: (A) افزودن کلید عمومی `~/.ssh/id_ed25519.pub` به GitHub SSH keys، یا (B) تغییر remote به HTTPS + token.

## Latest Status
**معماری دستگاه واقعی پیاده‌سازی شد** (درخواست PM): بک‌اند FastAPI + PostgreSQL (پورت ۵۴۳۳ در محیط dev برای جلوگیری از تداخل با کلاستر موجود ۵۴۳۲) + MQTT (paho) + WebSocket (uvicorn[standard]+websockets). الگوریتم پردازش رویدادهای خام دستگاه (۱۲ ستون) پیاده‌سازی و تست شد: ثبت ورود پرنده با RFID → وزن اولیه + timestamp دقیق، محاسبه feed_intake_g از افت لودسل مخزن، شمارش بازدید، بستن خودکار وعده (رویداد exit یا ردیف پایانی یا گپ >۹۰s). دوره‌های پرورشی در جدول `cycles` با cascade کامل به `visits` و `device_logs` مدیریت می‌شوند. فرانت‌اند (`v-dev`) حالا شامل مدیر دوره + استریم زنده WS است. تست انتها‌به‌انتها موفق: REST ایجاد دوره → seed 150 بازدید → stats صحیح (۲۱۰/۲۴۹ بازدید بسته‌شده، age_day نسبت به تاریخ دوره) → WS پوش لحظه‌ای تایید شد.

## Next Action
- رفع بلوک دیپلوی گیت‌هاب (SSH key یا HTTPS) و پوش تغییرات به `main` → GitHub Pages.
- پیشنهادهای بعدی: MQTT broker واقعی (mosquitto) برای دستگاه سخت‌افزاری، Dockerize بک‌اند، README استقرار بک‌اند.

- 2026-08-24: نسخه ۳٫۱ UI/UX — چیدمان کاملاً ریسپانسیو (۳۶۰ تا ۱۴۴۰px بدون سرریز، تست‌شده با Chrome headless در ۶ بیک‌پوینت)، ResizeObserver برای نمودارها، حالت خالی فارم با CTA، دسترس‌پذیری (role/aria/focus-visible/reduced-motion)، و اصطلاح‌شناسی راستی‌آزمایی‌شده فارسی (تنش گرمایی، ضریب تبدیل خوراک، تغذیه همزمان، واحد آزمایشی) + انگلیسی آکادمیک.

- 2026-08-24: v3.2 — سیستم سایه لایه‌ای (shadow-box) روی کارت‌ها/دکمه‌ها/نمودارها/جدول‌ها در هر دو تم + دکمه ♻️ بازنشانی دوره با تأیید دومرحله‌ای (پاک‌سازی زنده/فارم/سناریو/آمار) + دکمه بازگشت طرح آزمایش به پیش‌فرض.

- 2026-08-24: v3.3 — تگ اختصاصی هر پرنده روی نقشه (قابل خاموش/روشن) + کلیک روی هر جوجه → پروفایل فردی (وزن فعلی vs PO جنسیتی، کل مصرف، FCR فردی، CV ژنتیکی، وضعیت) + شیت «Per-bird records» در خروجی اکسل + رفع نقص عملکردی منوی همبرگری (باز می‌ماند، اعضا فعال، ✕/Esc/Backdrop برای بستن) + تکمیل کلیدهای انگلیسی جاافتاده.

- 2026-08-24: v3.4 — تکمیل کامل i18n: ریشه‌یابی ۷۶ نقطه هاردکد فارسی در app.js و مسیردهی به دیکشنری (~۴۰ کلید جدید FA/EN)، بازتولید کامل بخش‌های داینامیک هنگام تعویض زبان (داشبورد/فارم/اینسپکتور/بایو/سناریو/تیکر/طراح)، نام شیتهای اکسل به انگلیسی استاندارد + برچسب تیمارها دوزبانه در موتور (labelEn). امنیت: پاک‌سازی charset شناسه پن (موتور+UI)، escape همه interpolationهای HTML، گارد تزریق فرمول اکسل/CSV، ممیزی eval/XSS/noopener. تست با کلیک ماوس واقعی puppeteer: EN scan=۰ باقی‌مانده فارسی، XSS/CSV ✓، کل رگرسیون‌ها سبز.

- 2026-08-24: v1.0.0 نهایی — تب روش‌شناسی آکادمیک (چکیده، خط تولید، معادلات، ماتریکس صحت ۴ سویه، آمار، عدم‌قطعیت، تکرارپذیری) + انتخاب ستون‌های خروجی + رفع باگ click-blocker آموزش + i18n EN کامل.

- 2026-08-25: v1.0.0 نهایی — تب روش‌شناسی آکادمیک (چکیده، خط تولید، معادلات، ماتریکس صحت ۴ سویه، آمار، عدم‌قطعیت، تکرارپذیری) + انتخاب ستون‌های خروجی + رفع باگ ریشه‌ای click-blocker آموزش + i18n EN کامل + فلش RTL + HTML 깔끔.

## Summary History
- 2026-08-23: نسخه اول کامل — تحقیق ۸ منبع معتبر + شبیه‌ساز + اعتبارسنجی 0.79% MAE + گزارش.
- 2026-08-23: وب‌اپلیکیشن RossSim — موتور JS وفادار به simulator.py + طراحی مدرن RTL + سناریوها.
- 2026-08-23: نسخه ۲ — طراح آزمایش (پن/تعداد/تیمار)، نقشه فارم پویا، خروجی Excel+CSV.
- 2026-08-23: نسخه ۳ BroilerLab — کاتالوگ ۴ سویه از منابع رسمی، آمار زیستی کامل، دوزبانه FA/EN، مالکیت Arsalan Rezazadeh، بازطراحی آکادمیک.
