/* Arian — single source of truth for the app version.
   Bump SITE_VERSION here (once) on every release; every version badge
   (header pills, footer, about card) follows automatically. */
window.SITE_VERSION = "1.8.5";
